name: t_pdf_reader
description: "pdf reader witten by 'than_pdf_engine' package."
version: 0.0.1
homepage: https://github.com/ThanCoder/t_pdf_reader.git
repository: https://github.com/ThanCoder/t_pdf_reader.git

publish_to: none # လောလောဆယ် public မလုပ်သေးဘူး

environment:
  sdk: ^3.12.0
  flutter: ">=3.3.0"

dependencies:
  flutter:
    sdk: flutter
  dart_core_extensions: ^0.3.0
  than_pdf_engine:
    git: https://github.com/ThanCoder/than_pdf_engine.git
    # path: /home/thancoder/projects/flutter_ffi_projects/than_pdf_engine

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0
